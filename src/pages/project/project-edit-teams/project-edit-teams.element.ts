import { LitElement, html, css } from 'lit';

export class ProjectEditTeams extends LitElement {
  static styles = css`
    :host {
      display: flex;
      justify-content: center;
      align-items: center;
    }
  `;

  render() {
    return html`Teams and stuff`;
  }
}
